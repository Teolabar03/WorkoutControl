node_modules
*.log

# Il progetto Android e' versionato (android/), ma i file che `cap sync`
# rigenera a ogni build no: li ignora gia' android/.gitignore.

# Chiavi di firma: non devono finire nel repo (vivono in un secret GitHub).
*.keystore
*.jks
