{
  "name": "workoutcontrol-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "App Android di WorkoutControl: impacchetta il frontend e parla con l'API della VPS.",
  "scripts": {
    "build:web": "cd ../frontend && npm run build",
    "sync": "cap sync android",
    "apk": "npm run build:web && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/local-notifications": "8.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "typescript": "~6.0.2"
  }
}
