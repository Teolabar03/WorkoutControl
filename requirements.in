Flask>=3.1
Flask-SQLAlchemy>=3.1
Flask-CORS>=4.0
marshmallow>=3.21
python-dotenv>=1.0

# Provider per l'assistente AI: ne basta uno, l'app usa quello per cui hai
# configurato la chiave. Puoi rimuovere quello che non ti serve.
anthropic==0.122.0
google-genai==2.18.1
# Provider locale: nessuna chiave, richiede Ollama in esecuzione sul PC.
ollama>=0.4

# Server locale di produzione, anche su Windows.
waitress>=3.0.2
